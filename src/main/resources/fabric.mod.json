{
  "schemaVersion": 1,
  "id": "${id}",
  "version": "${version}",

  "name": "${name}",
  "description": "Backport the feature that authlib support reading service url overrides from java system properties to MC 1.16-",
  "authors": [
    "Fallen_Breath"
  ],
  "contact": {
    "homepage": "https://github.com/Fallen-Breath/AuthlibEnvBackport",
    "sources": "https://github.com/Fallen-Breath/AuthlibEnvBackport"
  },

  "icon": "assets/authlibenvbackport/icon.png",

  "environment": "*",
  "entrypoints": {
    "main": [
      "me.fallenbreath.authlibenvbackport.AuthlibEnvBackportMod"
    ]
  },
  "mixins": [
    "authlibenvbackport.mixins.json"
  ],

  "depends": {
    "fabricloader": ">=0.14",
    "minecraft": "${minecraft_dependency}"
  }
}
